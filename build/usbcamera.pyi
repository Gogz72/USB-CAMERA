# This file was generated by Nuitka

# Stubs included by default
from __future__ import annotations
from PIL import Image
from pathlib import Path
from pygrabber.dshow_graph import FilterGraph
from typing import Any, Optional, Tuple
from typing_extensions import Self
import cv2
import json
import os
import subprocess
import tempfile
import threading
import time

class UsbCamera:
    def __init__(self: Self, camera_index: Any, logger: Any, auto_reconnect: Any, image_save_path: Any, video_save_path: Any) -> None: ...
    def _log(self: Self, msg: Any, level: Any) -> Any: ...
    def _set_state(self: Self, state: str) -> Any: ...
    def get_state(self: Self) -> str: ...
    @staticmethod
    def _read_test_frame(cap: Any, timeout: float, sleep_interval: float) -> Tuple[bool, Any]: ...
    @classmethod
    def _open_cv_capture_candidates(cls, camera_ref: Any) -> Any: ...
    @classmethod
    def list_cameras(cls, max_test: int, ttl_seconds: float) -> list: ...
    @classmethod
    def list_camera_details(cls, max_test: int, read_timeout: float) -> dict: ...
    @staticmethod
    def _list_windows_pnp_camera_devices() -> list: ...
    @staticmethod
    def _auto_detect_camera(max_test: int) -> int: ...
    def set_camera(self: Self, index: Any) -> Any: ...
    def get_status(self: Self) -> dict: ...
    @classmethod
    def _get_camera_names_map(cls, ttl_seconds: float) -> Any: ...
    def _get_camera_name(self: Self, index: Any) -> str: ...
    def _open_capture(self: Self) -> bool: ...
    def _resolve_directshow_index(self: Self) -> Any: ...
    def _open_pygrabber_capture(self: Self) -> bool: ...
    def _is_capture_open(self: Self) -> bool: ...
    def _close_capture(self: Self) -> Any: ...
    def _capture_loop(self: Self, read_interval: float) -> Any: ...
    def start_capture(self: Self, background: bool, wait_ready: bool, timeout: float) -> Any: ...
    def stop_capture(self: Self, join: bool, timeout: float) -> Any: ...
    def is_capturing(self: Self) -> bool: ...
    def capture_image(self: Self, save_path: Optional[str], convert_rgb: bool) -> str: ...
    def get_last_image_path(self: Self) -> Optional[str]: ...
    def get_last_frame(self: Self) -> Optional[Tuple[bytes, float]]: ...
    def record_video(self: Self, output_path: Optional[str], duration: Optional[float], fps: Optional[float], codec: str) -> Any: ...
    def stop_recording(self: Self) -> Any: ...
    def stream_frames(self: Self, width: int, height: int, fps: float) -> Any: ...
    def set_resolution(self: Self, width: int, height: int) -> Any: ...
    def get_resolution(self: Self) -> Optional[Tuple[int, int]]: ...
    def set_property(self: Self, prop_id: int, value: float) -> Any: ...
    def get_property(self: Self, prop_id: int) -> Optional[float]: ...
    def _timestamp(self: Self) -> Any: ...
    def release(self: Self) -> Any: ...
    def __enter__(self: Self) -> Any: ...
    def __exit__(self: Self, exc_type: Any, exc_val: Any, exc_tb: Any) -> Any: ...


__name__ = ...



# Modules used internally, to allow implicit dependencies to be seen:
import os
import json
import subprocess
import tempfile
import threading
import time
import pathlib
import typing
import PIL
import PIL.Image
import cv2
import pygrabber
import pygrabber.dshow_graph
import ntpath